CREATE TABLE Guest(
    Username varchar(255) NOT NULL,
    Password varchar(255) NOT NULL,
    Firt varchar(50) NOT NULL,
    Last varchar(50) NOT NULL,
    Email varchar(62) NOT NULL,
    PRIMARY KEY (Username)
)